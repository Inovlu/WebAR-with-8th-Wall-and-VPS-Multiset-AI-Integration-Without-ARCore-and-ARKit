Instalation Guide:

1) Install node.js
2) Run in your terminal "npm install"
3) Duplicate ".env.example" and rename to ".env". Multiset AI provide its credentials. 
4) Run "npm run build" if you want to upload to website.
